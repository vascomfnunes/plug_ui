#pragma once

#include "PlugKnob.h"

namespace PlugUI {

/** Small knob — identical API to PlugKnob; the actual size is set by the
    caller via setBounds. This alias exists for semantic clarity. */
using PlugSmallKnob = PlugKnob;

} // namespace PlugUI
